librariesDirectory := $(shell pwd)/lib/linux-arm
includesDirectory := $(shell pwd)/include
sourcesDirectory := $(shell pwd)/source
applicationSuffix =
applicationDirectory := $(shell pwd)/apps
binariesDirectory := $(shell pwd)/bin/linux-arm
sdlconfig = sdl2-config
compiler = arm-linux-gnu-g++
compilerOptions = -Wall -I$(includesDirectory) -c `$(sdlconfig) --cflags` $(debugFlag)
linker = arm-linux-gnu-g++
linkerOptions = `$(sdlconfig) --libs` -lGL -lGLU -lSDL2_image
